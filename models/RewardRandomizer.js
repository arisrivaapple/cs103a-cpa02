'use strict';
const mongoose = require( 'mongoose' );
const Schema = mongoose.Schema;

var rewardRandomizerSchema = Schema( {
    
});

module.exports = mongoose.model('RewardRandomizer', rewardRandomizerSchema);